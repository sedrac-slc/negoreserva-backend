input UserResetPasswordLoggedInput {
    currentPassword: String!
    newPassword: String!
    confirmPassword: String!
}

extend type Mutation {
    userResetPassword(request: UserResetPasswordLoggedInput!): Boolean
}
